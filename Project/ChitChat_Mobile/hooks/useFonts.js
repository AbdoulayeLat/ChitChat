import * as Font from 'expo-font';
import { Inter_300Light } from '@expo-google-fonts/roboto';

export default useFonts = async () => {
  await Font.loadAsync({
    Roboto_400Regular: Roboto_400Regulart,
  });
};
